server:
  port: 8080

spring:
  application:
    name: smartload-optimizer

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      show-details: always

